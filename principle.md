# 单目摄像头人体身高测量 — 原理说明

> **版本**: 1.0  
> **依赖方法**: 透视投影 · 射线-平面相交 · 垂直站立约束

---

## 目录

1. [问题定义](#1-问题定义)
2. [坐标系约定](#2-坐标系约定)
3. [相机成像模型](#3-相机成像模型)
4. [算法推导](#4-算法推导)
5. [单应性矩阵方法](#5-单应性矩阵方法)
6. [误差分析](#6-误差分析)
7. [参数标定说明](#7-参数标定说明)
8. [实现要点与注意事项](#8-实现要点与注意事项)

---

## 1. 问题定义

给定一台**已标定**的固定单目摄像头（已知内参矩阵 $K$ 和外参 $R, \mathbf{t}$），以及图像中某一直立行人的：

- **脚底像素坐标** $(u_f, v_f)$：人与地面的接触点
- **头顶像素坐标** $(u_h, v_h)$：头部最高点

目标是恢复该行人的**真实身高** $H$（单位：米）。

> **核心前提**
> 1. 相机已完成内外参标定（或已知地面单应性矩阵）
> 2. 地面为平坦平面，世界坐标系中 $Z = 0$
> 3. 人体**直立站立**，头顶在脚底正上方（$X, Y$ 相同，$Z = H$）

---

## 2. 坐标系约定

```
世界坐标系                      相机坐标系 (OpenCV)
      Z↑                               Y↓
      |                                |
      |                                |
      o──── Y (depth)                  o──── X (right)
     /                                /
    X (right)                        Z (into scene)
```

| 坐标系 | X | Y | Z |
|--------|---|---|---|
| 世界 (World) | 向右 | 向前（深度） | **向上** |
| 相机 (Camera, OpenCV) | 向右 | **向下** | 向前 |

**地面平面**: 世界坐标系中 $Z_w = 0$ 的平面，法向量 $\mathbf{n} = (0, 0, 1)^T$。

---

## 3. 相机成像模型

### 3.1 完整投影流程

$$
\underbrace{[u \ v \ 1]^T}_{\text{像素坐标（齐次）}} \sim \quad
\underbrace{K}_{\text{内参}} \,
\underbrace{[R \mid \mathbf{t}]}_{\text{外参}} \,
\underbrace{[X_w \ Y_w \ Z_w \ 1]^T}_{\text{世界坐标（齐次）}}
$$

展开为：

$$
\begin{bmatrix} u'\\ v'\\ w \end{bmatrix}
= K (R \mathbf{P}_w + \mathbf{t}), \qquad u = \frac{u'}{w},\ v = \frac{v'}{w}
$$

### 3.2 内参矩阵 $K$

$$
K = \begin{bmatrix} f_x & s & c_x \\ 0 & f_y & c_y \\ 0 & 0 & 1 \end{bmatrix}
$$

| 参数 | 含义 |
|------|------|
| $f_x, f_y$ | 像素焦距（与物理焦距及像素尺寸有关） |
| $c_x, c_y$ | 主点（光轴与像平面交点） |
| $s$ | 畸变系数（相机歪斜，通常为 0） |

### 3.3 外参：$R$ 和 $\mathbf{t}$

| 符号 | 含义 |
|------|------|
| $R$ | $3\times3$ 旋转矩阵，将**世界向量**转到**相机坐标系** |
| $\mathbf{t}$ | $3\times1$ 平移向量（相机坐标系下世界原点的位置） |
| $\mathbf{C}_w = -R^T \mathbf{t}$ | **相机光心**在世界坐标系中的位置 |

---

## 4. 算法推导

### Step 1 — 脚底反投影：像素 → 3D 射线

由透视投影的可逆性，给定像素 $(u, v)$，可以确定一条过相机光心的三维射线：

$$
\mathbf{d}_{cam} = K^{-1} \begin{bmatrix} u \\ v \\ 1 \end{bmatrix}, \qquad
\mathbf{d}_{world} = R^T \mathbf{d}_{cam}
$$

射线的参数方程为：

$$
\mathbf{P}(s) = \mathbf{C}_w + s \cdot \hat{\mathbf{d}}_{world}, \quad s > 0
$$

其中 $\hat{\mathbf{d}}_{world} = \mathbf{d}_{world} / \|\mathbf{d}_{world}\|$。

### Step 2 — 脚底与地面的交点

地面方程：$Z_w = 0$，即 $\mathbf{P}(s) \cdot \mathbf{z} = 0$（$\mathbf{z} = [0,0,1]^T$）：

$$
C_z + s \cdot d_z = 0 \implies s^* = -\frac{C_z}{d_z}
$$

**校验**：$s^* > 0$ 要求 $C_z$ 与 $d_z$ 异号，即相机在地面上方且射线朝下。

脚底 3D 位置：

$$
\boxed{
\mathbf{P}_{foot} = \mathbf{C}_w + s^* \hat{\mathbf{d}}_{foot}
= \left(-\frac{C_z}{d_{foot,z}}\right) \hat{\mathbf{d}}_{foot} + \mathbf{C}_w
}
$$

### Step 3 — 头顶 3D 位置（垂直站立约束）

对头顶像素 $(u_h, v_h)$ 同理得到射线方向 $\hat{\mathbf{d}}_{head}$。

由**人体直立假设**：

$$
P_{head,X} = P_{foot,X}, \quad P_{head, Y} = P_{foot, Y}
$$

联立射线方程 $P_{head,i} = C_i + s \cdot d_{head,i}$，可解出参数 $s$：

$$
s \cdot d_{head,i} = P_{foot,i} - C_i, \quad i \in \{X, Y\}
$$

**稳健解法（加权最小二乘）**：同时利用 $X$ 和 $Y$ 约束：

$$
\boxed{
s^* = \frac{d_{head,X}(P_{foot,X} - C_X) + d_{head,Y}(P_{foot,Y} - C_Y)}{d_{head,X}^2 + d_{head,Y}^2}
}
$$

（等价于对两个方程做最小二乘，残差 = 噪声带来的偏差）

### Step 4 — 身高计算

$$
\boxed{
H = P_{head,Z} - P_{foot,Z} = (\mathbf{C}_w + s^* \hat{\mathbf{d}}_{head})_Z - P_{foot,Z}
}
$$

由于 $P_{foot,Z} = 0$（在地面上），化简为：

$$
H = C_Z + s^* \cdot d_{head,Z}
$$

### 几何直觉图

```
              camera C
             /|\
            / | \
           /  |  \
          /   |   \
---------+----+----+--------  Z=0 (地面)
      foot  (通过 H 点正上方)  head_ground
      
      脚底射线与地面相交 → P_foot
      头顶射线通过垂直约束定位 → P_head
      H = P_head.Z - P_foot.Z
```

---

## 5. 单应性矩阵方法

### 5.1 地面单应性

地面上任意点 $Z_w = 0$，消去第三列后，投影关系退化为：

$$
\mathbf{p}_{img} \sim K [r_1 \ r_2 \ \mathbf{t}] \begin{bmatrix} X_w \\ Y_w \\ 1 \end{bmatrix}
\triangleq H \begin{bmatrix} X_w \\ Y_w \\ 1 \end{bmatrix}
$$

其中 $H = K [r_1, r_2, \mathbf{t}]$（$r_1, r_2$ 为 $R$ 的第一、二列）。

### 5.2 用单应性定位脚底

$$
\begin{bmatrix} X_w \\ Y_w \\ 1 \end{bmatrix} \sim H^{-1} \begin{bmatrix} u_f \\ v_f \\ 1 \end{bmatrix}
$$

**注意**：单应性只能恢复地面点坐标，无法直接给出身高。高度计算仍需配合相机参数（或第 4 节的射线法）。

### 5.3 单应性的优势

- 可以仅通过**4 对图像-地面点对应**标定，无需精确的内外参分解
- 适合地面已有标志点（地面标定板、车道线等）的场景

---

## 6. 误差分析

### 6.1 误差来源

| 误差源 | 影响 | 备注 |
|--------|------|------|
| 像素检测误差 $\delta_{px}$ | 主要误差，线性传播 | 人体关键点检测/人工标注 |
| 相机标定误差 | 系统误差 | 内外参精度决定上限 |
| 地面平坦假设 | 地面起伏引入 | 室内/受控场景影响小 |
| 人体非刚直 | 低估误差 | 倾斜行走时 |
| 镜头畸变（未纠正） | 边缘区域明显 | 需先进行畸变校正 |

### 6.2 像素误差的传播

对身高估计值 $\hat{H}$ 关于脚底像素 $(u_f, v_f)$ 作一阶泰勒展开：

$$
\delta H \approx \frac{\partial H}{\partial v_f} \delta v_f + \frac{\partial H}{\partial v_h} \delta v_h
$$

**关键结论**：
- 远处目标（小 $v_f$，远离图像底部）的 $\partial H / \partial v$ 更大，误差放大更显著
- 增大焦距 $f_y$ 可以减小每像素对应的实际角度，从而提高测量精度
- 头顶检测误差通常比脚底误差影响更大（头顶更难准确定位）

### 6.3 经验误差参考

在典型监控相机场景（安装高度 3–5 m，焦距 1000–1500 px，目标距离 5–20 m）下：

| 检测噪声 | 均值误差 | 最大误差 |
|----------|----------|----------|
| 0 px（理论） | < 0.01 mm | < 0.01 mm |
| 1 px | ~ 1 cm | ~ 3 cm |
| 3 px | ~ 3 cm | ~ 9 cm |
| 5 px | ~ 5 cm | ~ 15 cm |

---

## 7. 参数标定说明

### 7.1 内参标定（$K$）

使用棋盘格标定板（OpenCV `calibrateCamera`）：
1. 拍摄多角度、多位置的棋盘格图像（至少 15–20 张）
2. 检测角点，调用标定函数
3. 得到 $K$、畸变系数 $[k_1, k_2, p_1, p_2]$
4. 对所有待处理图像先进行畸变校正

### 7.2 外参标定（$R, \mathbf{t}$）

**方法 A：地面标定板**
- 在地面放置已知尺寸的标定板
- 使用 `solvePnP` 求解 $R, \mathbf{t}$

**方法 B：已知地面点对应（单应性）**
- 测量地面上 4+ 个标志点的世界坐标
- 用 `findHomography` 求 $H$，再分解出 $R, \mathbf{t}$

**方法 C：几何测量**
- 精确测量相机安装高度 $H_c$
- 测量摄像头俯仰角 $\theta$（用倾角仪）
- 构造旋转矩阵（见代码 `from_look_at`）

### 7.3 标定精度要求

对于厘米级身高测量，建议：
- 内参重投影误差 $< 0.5$ px
- 地面标定点残差 $< 1$ cm
- 相机安装高度测量误差 $< 2$ cm

---

## 8. 实现要点与注意事项

### ✅ 必须满足的条件

1. **脚底点必须在地面上**：若目标站在台阶、坡道等非地面区域，需修正 $Z_{foot}$
2. **图像需先进行畸变校正**：或使用畸变矫正后的等效参数
3. **人体检测框的头顶/脚底定位**：建议使用姿态估计（如关键点检测）而非边界框顶底

### ⚠️ 数值稳定性

- 当 $d_{foot,z} \approx 0$（射线近似水平），`intersect_ground` 会失败 → 须检查仰角
- 当头顶射线 $d_{head,X} \approx d_{head,Y} \approx 0$（对应图像中心正下方），垂直约束退化 → 罕见但须处理

### 🔧 工程优化建议

1. **批量处理**：对视频流可复用 `unproject_ray` 的部分计算（仅在 $K, R, t$ 不变时）
2. **卡尔曼滤波**：对连续帧的身高估计进行时序滤波，降低抖动
3. **多视角融合**：若有多个相机，取加权平均可显著提升精度
4. **距离自适应**：近处目标精度高，可据目标距离自适应调整置信度

---

## 参考公式汇总

$$
\begin{aligned}
\text{脚底3D位置:} \quad & \mathbf{P}_{foot} = \mathbf{C}_w - \frac{C_Z}{d_{foot,Z}} \hat{\mathbf{d}}_{foot} \\[6pt]
\text{头顶参数解:} \quad & s^* = \frac{\mathbf{d}_{head}^{XY} \cdot (\mathbf{P}_{foot}^{XY} - \mathbf{C}_w^{XY})}{\|\mathbf{d}_{head}^{XY}\|^2} \\[6pt]
\text{估计身高:} \quad & H = C_Z + s^* \cdot d_{head,Z}
\end{aligned}
$$

其中上标 $XY$ 表示取向量的前两个分量。
