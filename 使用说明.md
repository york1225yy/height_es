# face_recognition 人脸识别项目使用说明

## 项目简介

本项目基于 [ageitgey/face_recognition](https://github.com/ageitgey/face_recognition) 库，该库是目前最简单易用的 Python 人脸识别库之一，底层使用 [dlib](http://dlib.net/) 的深度学习模型，在 Labeled Faces in the Wild 测试集上准确率高达 **99.38%**。

`demo.py` 是一个功能完整的演示脚本，支持：
- 从**摄像头**或**视频文件**读取输入
- 使用 **CPU（HOG 模型）** 或 **GPU（CNN 模型）** 进行人脸检测
- 实时人脸识别，将检测到的人脸与已知人脸数据库进行比对

---

## 环境要求

| 依赖项 | 版本要求 |
|--------|---------|
| Python | >= 3.6  |
| cmake  | 系统级安装 |
| dlib   | >= 19.7 |
| face_recognition | 最新版 |
| opencv-python | >= 4.0 |
| numpy  | >= 1.17 |

---

## 安装步骤

### 1. 安装系统依赖（Ubuntu/Debian）

```bash
sudo apt-get update
sudo apt-get install -y cmake libopenblas-dev liblapack-dev
```

### 2. 安装 Python 依赖

```bash
pip install face_recognition opencv-python numpy
```

### 3. GPU 加速安装（可选，需要 NVIDIA GPU）

若要使用 GPU 加速，需要：

1. 安装 CUDA Toolkit（推荐 CUDA 11.x）：参考 [NVIDIA CUDA 安装指南](https://developer.nvidia.com/cuda-downloads)

2. 安装支持 CUDA 的 dlib（需从源码编译）：

```bash
# 先卸载已有的 dlib
pip uninstall dlib

# 从源码编译安装（需要 CUDA 环境）
git clone https://github.com/davisking/dlib.git
cd dlib
python setup.py install --set DLIB_USE_CUDA=1
```

3. 安装其余依赖：

```bash
pip install face_recognition opencv-python numpy
```

---

## 准备已知人脸数据

在运行 `demo.py` 之前，你需要准备已知人脸的图片：

1. 在项目根目录下创建 `known_faces/` 文件夹

```bash
mkdir known_faces
```

2. 将已知人脸图片放入该文件夹，**文件名即为人物姓名**（支持中英文）：

```
known_faces/
├── 张三.jpg
├── 李四.png
├── obama.jpg
└── biden.jpg
```

> **提示**：每张图片最好只包含一张清晰的正面人脸，图片分辨率建议不低于 200×200 像素。

---

## 运行方式

### 基本语法

```bash
python demo.py [参数选项]
```

### 参数说明

| 参数 | 简写 | 默认值 | 说明 |
|------|------|--------|------|
| `--input` | `-i` | `camera` | 输入源：`camera` 使用摄像头，或指定视频文件路径 |
| `--device` | `-d` | `cpu` | 推理设备：`cpu` 或 `gpu` |
| `--camera-id` | `-c` | `0` | 摄像头编号（仅在 `--input camera` 时有效） |
| `--scale` | `-s` | `0.25` | 视频帧缩放比例（越小越快，范围 0.1~1.0） |
| `--tolerance` | `-t` | `0.6` | 人脸匹配容差（越小越严格，范围 0.0~1.0） |
| `--known-faces` | `-k` | `known_faces` | 已知人脸图片所在目录路径 |
| `--output` | `-o` | （不输出） | 结果视频保存路径（可选）|
| `--skip-frames` | `-f` | `1` | 每隔几帧处理一次（1 表示每帧都处理）|

### 使用示例

**示例 1：使用摄像头 + CPU 模式（最基本用法）**

```bash
python demo.py
```

**示例 2：使用摄像头 + GPU 模式**

```bash
python demo.py --device gpu
```

**示例 3：读取视频文件 + CPU 模式**

```bash
python demo.py --input ./video/test.mp4
```

**示例 4：读取视频文件 + GPU 模式，并保存结果**

```bash
python demo.py --input ./video/test.mp4 --device gpu --output result.avi
```

**示例 5：使用第二个摄像头，降低匹配容差**

```bash
python demo.py --camera-id 1 --tolerance 0.5
```

**示例 6：提高处理速度（降低分辨率 + 隔帧处理）**

```bash
python demo.py --scale 0.25 --skip-frames 2
```

---

## 运行时操作

程序运行后会弹出视频窗口，支持以下按键：

| 按键 | 功能 |
|------|------|
| `q`  | 退出程序 |
| `s`  | 截图保存当前帧 |

---

## 识别结果说明

- **彩色矩形框**：表示检测到的人脸位置
  - 🟢 绿色：识别到已知人物
  - 🔴 红色：未知人物
- **文字标签**：显示匹配的人名及相似度得分
  - 格式：`姓名 (相似度%)` 或 `Unknown`

---

## CPU 与 GPU 模式对比

| 特性 | CPU 模式（HOG）| GPU 模式（CNN）|
|------|--------------|--------------|
| 检测模型 | HOG + SVM | CNN 深度学习 |
| 速度 | 较快 | 更快（需 CUDA）|
| 精度 | 一般 | 更高 |
| 遮挡处理 | 较差 | 较好 |
| 侧脸检测 | 较差 | 较好 |
| 硬件要求 | 仅需 CPU | 需要 NVIDIA GPU + CUDA |

> **注意**：若指定 `--device gpu` 但系统未安装支持 CUDA 的 dlib，程序会自动回退到 CPU 模式并给出提示。

---

## 常见问题

### Q1：运行时报错 `No module named 'face_recognition'`

```bash
pip install face_recognition
```

### Q2：运行时报错 `No module named 'cv2'`

```bash
pip install opencv-python
```

### Q3：摄像头无法打开

- 检查摄像头是否被其他程序占用
- 尝试更改 `--camera-id` 参数（如 `--camera-id 1`）
- Linux 下确认用户有 `/dev/video0` 访问权限

### Q4：GPU 模式没有加速效果

- 检查 dlib 是否以 CUDA 支持编译：

```python
import dlib
print(dlib.DLIB_USE_CUDA)  # 应输出 True
```

### Q5：人脸识别不准确

- 增加已知人脸图片数量（每人多张、不同角度）
- 调整 `--tolerance` 参数（减小可提高精度，但可能增加未识别情况）
- 使用 `--device gpu` 切换到 CNN 模型提高检测精度

---

## 项目文件结构

```
height_es/
├── demo.py                    # 主演示脚本
├── 使用说明.md                # 本使用说明文档
├── known_faces/               # 已知人脸图片目录（需手动创建）
│   ├── 张三.jpg
│   └── 李四.png
└── face_recognition/          # face_recognition 库源代码
    ├── face_recognition/      # 核心 Python 模块
    ├── examples/              # 官方示例代码
    ├── README.md              # 官方英文文档
    └── README_Simplified_Chinese.md  # 官方中文文档
```

---

## 参考资料

- [face_recognition 官方文档](https://github.com/ageitgey/face_recognition)
- [dlib 官网](http://dlib.net/)
- [dlib CUDA 编译指南](https://gist.github.com/ageitgey/629d75c1baac34dfa5ca2a1928a7aeaf)
