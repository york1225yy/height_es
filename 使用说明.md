# face_recognition 人脸识别项目使用说明

## 项目简介

本项目基于 [ageitgey/face_recognition](https://github.com/ageitgey/face_recognition) 库，该库是目前最简单易用的 Python 人脸识别库之一，底层使用 [dlib](http://dlib.net/) 的深度学习模型，在 Labeled Faces in the Wild 测试集上准确率高达 **99.38%**。

`demo.py` 是一个功能完整的演示脚本，支持：
- 从**摄像头**或**视频文件**读取输入
- 使用 **CPU（HOG 模型）** 或 **GPU（CNN 模型）** 进行人脸检测
- 实时人脸识别，将检测到的人脸与已知人脸数据库进行比对
- **无头模式（Headless）**：支持在无显示器的服务器（AutoDL、云服务器）上直接运行

> Jetson Orin Nano 专用运行说明请查看：`Jetson使用说明.md`

---

## 已验证的依赖版本

> **重要提示**：请严格使用以下版本，版本不兼容会导致运行报错。

| 包名 | 版本 | 说明 |
|------|------|------|
| Python | **3.8.20** | 须使用 Python 3.8，Python 3.12 因 `pkg_resources` 问题无法运行 |
| dlib | **19.24.6** | dlib 20.x API 与 face_recognition 1.3.0 不兼容，须使用 19.x |
| face-recognition | **1.3.0** | |
| face-recognition-models | **0.3.0** | 须从 GitHub 安装，包含 dlib 预训练模型文件 |
| opencv-python | **4.13.0.92** | |
| numpy | **1.24.4** | |
| pillow | **10.4.0** | |
| click | **8.1.8** | |
| setuptools | **75.3.4** | |
| wheel | **0.45.1** | |

---

## 一键安装（推荐）

项目提供 `install.sh` 脚本，可在 Ubuntu 系统（包括 AutoDL、本地机器、云服务器）上一键完成所有依赖安装。

```bash
# 1. 克隆项目
git clone https://github.com/york1225yy/height_es.git
cd height_es

# 2. 赋予执行权限并运行安装脚本
chmod +x install.sh
./install.sh
```

安装过程约需 **15~25 分钟**（主要耗时在编译 dlib，约 10~15 分钟）。

安装完成后，激活虚拟环境：

```bash
source activate_env.sh
# 或
source .venv/bin/activate
```

---

## 手动安装步骤

### 1. 安装系统依赖（Ubuntu/Debian）

```bash
sudo apt-get update
sudo apt-get install -y \
    build-essential cmake \
    libopenblas-dev liblapack-dev \
    libx11-dev libgtk-3-dev \
    libboost-python-dev \
    python3-dev git curl
```

### 2. 安装 uv（Python 版本与包管理工具）

```bash
curl -LsSf https://astral.sh/uv/install.sh | sh
export PATH="$HOME/.local/bin:$PATH"
```

### 3. 安装 Python 3.8 并创建虚拟环境

```bash
uv python install 3.8
uv venv .venv --python 3.8
```

### 4. 安装 Python 依赖

```bash
# 基础包
uv pip install --python .venv/bin/python \
    "setuptools==75.3.4" "wheel==0.45.1" "numpy==1.24.4"

# 编译 dlib 19.24.6（约 10~15 分钟，请耐心等待）
uv pip install --python .venv/bin/python "dlib==19.24.6"

# face_recognition 预训练模型（从 GitHub 安装，包含 dlib 模型文件）
uv pip install --python .venv/bin/python \
    "git+https://github.com/ageitgey/face_recognition_models"

# 其余依赖
uv pip install --python .venv/bin/python \
    "face-recognition==1.3.0" \
    "opencv-python==4.13.0.92" \
    "pillow==10.4.0" \
    "click==8.1.8"
```

### 5. GPU 加速安装（可选，需要 NVIDIA GPU + CUDA）

1. 安装 CUDA Toolkit（推荐 CUDA 11.x 或 12.x）：
   参考 [NVIDIA CUDA 安装指南](https://developer.nvidia.com/cuda-downloads)

2. 编译支持 CUDA 的 dlib（替换步骤 4 的 dlib 安装命令）：

```bash
git clone https://github.com/davisking/dlib.git
cd dlib && git checkout v19.24.6
uv pip install --python .venv/bin/python . \
    --config-settings=cmake.args="-DDLIB_USE_CUDA=ON"
```

3. 验证 CUDA 支持：

```python
import dlib
print(dlib.DLIB_USE_CUDA)  # 应输出 True
```

### 6. 激活虚拟环境

```bash
source .venv/bin/activate
```

---

## 准备已知人脸数据

1. 在项目根目录下创建 `known_faces/` 文件夹：

```bash
mkdir -p known_faces
```

2. 将已知人脸图片放入该文件夹，**文件名（不含扩展名）即为人物姓名**（支持中英文）：

```
known_faces/
├── 张三.jpg
├── 李四.png
├── obama.jpg
└── yy.jpg        ← 示例：系统已包含此文件
```

> **提示**：每张图片只包含一张清晰的正面人脸，图片分辨率建议不低于 200×200 像素。

---

## 运行方式

### 基本语法

```bash
python demo.py [参数选项]
```

### 完整参数说明

| 参数 | 简写 | 默认值 | 说明 |
|------|------|--------|------|
| `--input` | `-i` | `camera` | 输入源：`camera` 使用摄像头，或指定视频文件路径 |
| `--device` | `-d` | `cpu` | 推理设备：`cpu` 或 `gpu` |
| `--camera-id` | `-c` | `0` | 摄像头编号（仅 `--input camera` 时有效） |
| `--scale` | `-s` | `0.25` | 视频帧缩放比例（越小越快，范围 0.1~1.0） |
| `--skip-frames` | `-f` | `1` | 每隔几帧处理一次（1=每帧；2=隔帧处理） |
| `--tolerance` | `-t` | `0.6` | 人脸匹配容差（越小越严格，范围 0.0~1.0） |
| `--known-faces` | `-k` | `known_faces` | 已知人脸图片所在目录路径 |
| `--output` | `-o` | （不保存） | 结果视频保存路径（.mp4 格式） |
| `--no-display` | — | 自动检测 | 无头模式，不显示窗口（无显示器时自动启用） |

### 使用示例

**示例 1：摄像头实时识别（有显示器）**

```bash
python demo.py
```

**示例 2：摄像头 + GPU 模式**

```bash
python demo.py --device gpu
```

**示例 3：处理视频文件（服务器 / AutoDL 推荐）**

```bash
python demo.py --input ./video/test.mp4 --device cpu \
               --output ./output/result.mp4 --no-display
```

**示例 4：视频文件 + GPU 模式 + 保存结果**

```bash
python demo.py --input ./video/test.mp4 --device gpu \
               --output ./output/result.mp4 --no-display
```

**示例 5：使用第二个摄像头，提高匹配精度**

```bash
python demo.py --camera-id 1 --tolerance 0.5
```

**示例 6：提高处理速度（低分辨率 + 跳帧）**

```bash
python demo.py --input ./video/test.mp4 \
               --scale 0.25 --skip-frames 2 --no-display
```

**示例 7：使用自定义已知人脸目录**

```bash
python demo.py --input ./video/test.mp4 \
               --known-faces ./my_faces --no-display
```

---

## 无头模式（服务器 / AutoDL 运行）

在无显示器的环境（AutoDL、SSH 连接的服务器）下运行时，程序**自动检测**并启用无头模式：

- 检测到无 `DISPLAY` 环境变量时，自动添加 `--no-display`
- 无头模式下不调用 `cv2.imshow()`，不弹出窗口
- 每处理 50 帧在终端打印一次进度信息
- 结果视频正常写入 `--output` 指定路径

也可手动指定无头模式：

```bash
python demo.py --input ./video/test.mp4 --output ./output/result.mp4 --no-display
```

---

## 运行时操作（有显示器模式）

| 按键 | 功能 |
|------|------|
| `q`  | 退出程序 |
| `s`  | 截图保存当前帧到 `./screenshots/` |

---

## 识别结果说明

- **彩色矩形框**：表示检测到的人脸位置
  - 🟢 绿色：识别到已知人物
  - 🔴 红色：未知人物
- **文字标签**：`姓名 (相似度%)` 或 `Unknown`
- **左上角信息栏**：显示 FPS、推理设备、检测到的人脸数、已处理总帧数

---

## CPU 与 GPU 模式对比

| 特性 | CPU 模式（HOG）| GPU 模式（CNN）|
|------|--------------|--------------|
| 检测模型 | HOG + SVM | CNN 深度学习 |
| 速度参考 | ~9~15 FPS（4 核 CPU） | ~30+ FPS（依 GPU 性能） |
| 精度 | 一般（正面人脸） | 更高（侧脸、遮挡） |
| 遮挡处理 | 较差 | 较好 |
| 侧脸检测 | 较差 | 较好 |
| 硬件要求 | 仅需 CPU | NVIDIA GPU + CUDA |

> **注意**：若指定 `--device gpu` 但 dlib 未编译 CUDA 支持，程序自动回退到 CPU 模式。

---

## 常见问题

### Q1：安装 dlib 报错

确保已安装系统依赖后再安装 dlib：

```bash
sudo apt-get install -y cmake libopenblas-dev liblapack-dev libx11-dev
uv pip install --python .venv/bin/python "dlib==19.24.6"
```

### Q2：`TypeError: compute_face_descriptor(): incompatible function arguments`

dlib 20.x 修改了 pybind11 API，与 face_recognition 1.3.0 不兼容。必须使用 dlib **19.24.6**：

```bash
uv pip install --python .venv/bin/python "dlib==19.24.6"
```

### Q3：`No module named 'pkg_resources'`

这是 Python 3.12 + setuptools 82.x 的已知问题。请使用 **Python 3.8** 虚拟环境（参见安装步骤）。

### Q4：`No module named 'face_recognition'`

确认已激活虚拟环境后再运行：

```bash
source .venv/bin/activate
python demo.py ...
```

### Q5：摄像头无法打开

- 检查摄像头是否被其他程序占用
- 尝试 `--camera-id 1`
- Linux 下确认权限：`ls -la /dev/video*`

### Q6：GPU 模式没有加速效果

```python
import dlib
print(dlib.DLIB_USE_CUDA)  # 应输出 True，否则需重新编译 dlib 开启 CUDA
```

### Q7：人脸识别不准确

- 增加已知人脸图片数量（不同角度、不同光照）
- 减小 `--tolerance`（如 `--tolerance 0.5`）
- 使用 `--device gpu` 切换 CNN 模型
- 提高 `--scale`（如 `--scale 0.5`）以提高检测分辨率

### Q8：服务器上运行报显示相关错误

使用 `--no-display` 参数：

```bash
python demo.py --input ./video/test.mp4 --output ./output/result.mp4 --no-display
```

---

## 项目文件结构

```
height_es/
├── demo.py                    # 主演示脚本（CPU/GPU、摄像头/视频、无头模式）
├── install.sh                 # 一键安装脚本（Ubuntu / AutoDL）
├── activate_env.sh            # 虚拟环境激活脚本（install.sh 运行后自动生成）
├── 使用说明.md                # 本使用说明文档
├── README.md                  # 项目简介
├── known_faces/               # 已知人脸图片目录
│   └── yy.jpg                 # 示例：人名即文件名
├── video/                     # 输入视频目录
│   └── test.mp4               # 示例测试视频（1080x1920，108 帧）
├── output/                    # 输出结果目录
│   └── result_final.mp4       # 示例输出结果
├── .venv/                     # Python 3.8 虚拟环境（.gitignore 已排除）
└── face_recognition/          # face_recognition 库源代码
    ├── face_recognition/      # 核心 Python 模块
    ├── examples/              # 官方示例代码
    ├── README.md              # 官方英文文档
    └── README_Simplified_Chinese.md  # 官方中文文档
```

---

## 参考资料

- [face_recognition 官方文档](https://github.com/ageitgey/face_recognition)
- [dlib 官网](http://dlib.net/)
- [uv 包管理器](https://github.com/astral-sh/uv)
- [dlib CUDA 编译指南](https://gist.github.com/ageitgey/629d75c1baac34dfa5ca2a1928a7aeaf)
