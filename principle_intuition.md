# 单目摄像头身高测量 — 直觉原理说明

> 本文档不含数学公式，用白话和比喻解释算法背后的理论。

---

## 一、为什么单目相机能量物体高度？

很多人直觉上认为"一张照片只有宽和高两个维度，哪来的深度信息，谈何测量真实尺寸呢？"

答案的关键在于：**单靠照片确实无法测量，但一旦你额外告诉相机「地面在哪里」，一切就变得可能了。**

这和人类的视觉经验完全一样。你闭上一只眼睛看一张照片，完全无法判断照片里的人有多高。但是只要你知道那个人站在地面上、知道相机架在哪个位置和角度，你就能通过几何关系把高度算出来。

算法做的事情，就是把这个人类凭直觉做的推理，变成严格的几何计算。

---

## 二、三个核心前提

算法能成立，依赖以下三个前提：

**前提一：地面是平的。**  
我们假设被测量的人站在一个平坦的地面上。整个算法的基础就是"地面"这个参照平面。没有这个平面，就无法锚定脚底的真实位置。

**前提二：相机已经标定。**  
相机需要事先完成"标定"——弄清楚它的镜头特性（焦距、主点等，称为内参）以及它的安装姿态（高度、朝向，称为外参）。只有知道这些，才能把图像里的像素位置和真实世界的方向对应起来。

**前提三：人是直立站立的。**  
算法假设头顶在脚底的正上方，即头和脚的水平位置相同，两者之间的垂直距离就是身高。

---

## 三、相机标定参数的直觉含义

### 内参：镜头的"放大规律"

想象你拿着一根铅笔，伸直手臂放在眼前，铅笔在你的视野里占据了一定的角度。内参描述的就是：**图像里一个像素对应真实世界多大的角度**。

- **焦距（fx, fy）**：焦距越长（长焦镜头），同样远的物体在图像里占据的像素就越多，每个像素对应的角度就越小——测量就越精确。
- **主点（cx, cy）**：图像的"光学中心"，近似于图像的正中间。

内参告诉算法："图像坐标系里，一个像素的偏移，代表你的视线偏转了多少角度。"

### 外参：相机的"安装状态"

外参描述的是**相机被安装在哪里、对准哪里**，由两部分组成：

- **旋转（R）**：相机的朝向。是仰拍、俯拍、还是斜拍？水平旋转了多少？
- **平移（t）**：相机光心（镜头的"眼睛"所在位置）在世界坐标系里的绝对位置。

两者合起来回答一个问题：**真实世界里某一个点，在相机的视角里应该出现在什么方向？**

---

## 四、注视点（target）到底是什么？

在代码的 `from_look_at` 方法里有一个参数叫 `target`，示例值是地面上正前方 12 米处的一个点。

**它唯一的作用是：帮助计算旋转矩阵 R。**

具体地说：相机装在某个位置，镜头对准某个方向。"从相机位置指向注视点"这个方向就是"相机的朝向"。有了朝向和"相机不歪斜"的约定，就能严格地算出三个互相垂直的轴（右方向、下方向、前方向），从而构成旋转矩阵。

**注视点本身不参与任何后续的身高计算**，它在 `from_look_at` 里用完之后就"消失了"，取而代之的是算出来的 R 和 t。

换一句话说：如果你已经有了通过其他方式（如标定工具）得到的 R、t，完全不需要注视点，可以直接用 `HeightEstimator(K, R, t)` 构造。`from_look_at` 只是一个方便使用的快捷方式，适合"知道相机装在哪、对准哪"的场合。

---

## 五、算法执行的三步推理

### 第一步：确定脚在地面上的真实位置

图像里脚底的像素点，对应的是"从相机出发、穿过那个像素方向的一条射线"。这条射线射向无穷远处，真正的脚在射线上的某一个位置。

但我们知道脚在地面上，就把这个约束加进来：**射线与地面相交的那个点，就是脚的真实 3D 位置**。

这就像用激光笔从你眼睛的位置朝某个方向射出一道光，这道光打到地板上的位置，就是真实的脚底位置。

### 第二步：确定头顶的真实位置

类似地，头顶像素也对应一条从相机出发的射线。但头顶不在地面上，射线和地面的交点不是我们要找的答案。

这时候用上了"人是直立的"这个约束：头顶的水平位置和脚底完全相同，只是高度不同。

所以我们在头顶那条射线上，找那个"水平位置与脚底重合"的点——那就是头顶的真实 3D 位置。

### 第三步：计算高度差

有了脚底和头顶的真实 3D 坐标，把两者的高度（Z 方向）相减，就是身高。

---

## 六、为什么远处的人测量误差更大？

这个问题有很直观的答案。

想象你用手指遮住一个人的脚：在近处，你的手指盖住了他脚踝以下很少的部分；在远处，你的手指盖住了他整个人。

**距离越远，图像里每一个像素对应的真实世界距离就越大。**

当人体关键点检测有 2 个像素的误差时：
- 近处目标，2 像素大约对应 2 厘米
- 远处目标，2 像素可能对应 10 厘米甚至更多

俯仰角越小（相机看得越平），远处目标在图像里越"压缩"，误差放大越明显。这也是为什么监控相机要安装在足够高的位置并向下俯拍——既能覆盖更大区域，又能控制测量精度。

---

## 七、单应性矩阵是什么？

单应性矩阵（Homography）是一种特殊情况下的简化工具。

当我们只关心地面上的点的时候，地面是一个平面，从"地面上的真实坐标"到"图像中的像素坐标"的关系可以用一个简单的 3×3 矩阵完整描述，这就是单应性矩阵。

**直觉上**：单应性矩阵就像一个"地图投影"。你站在相机位置俯瞰地面，单应性矩阵告诉你地面的任何一个点投影到图像里在哪里；反过来，图像里任何一个地面上的点（比如脚底），也可以通过它"反投影"回到地面上的真实坐标。

单应性矩阵的好处是**不需要分别知道内参和外参**，只需要在地面上放 4 个以上已知真实坐标的标志点，就能直接标定出这个矩阵。这在实际工程中经常更方便。

缺点是单应性矩阵只能处理地面上的点，对于头顶这种不在地面上的点，它无能为力——身高计算的第二步还是需要借助完整的相机参数。

---

## 八、什么情况下算法会失效？

理解算法的前提，就能理解它的失效场景：

| 情况 | 原因 |
|------|------|
| 人站在台阶、斜坡、车顶上 | 脚底不在 Z=0 平面，地面交点计算出错 |
| 相机极度倾斜（近似水平安装） | 脚底射线和地面几乎平行，交点跑到极远处，数值不稳定 |
| 图像有明显的镜头畸变（鱼眼） | 直线变弯，内参假设不成立，需先校正畸变 |
| 人体严重倾斜（跌倒、弯腰） | 垂直约束不成立，头顶不在脚底正上方 |
| 脚底被遮挡 | 无法得到准确的脚底像素坐标 |

---

## 九、一句话总结

> **这个算法的本质，是用"人站在已知平面上"这个物理约束，把原本信息不足的单目图像问题，转化成了一个有唯一解的几何问题。**

相机标定给了算法"理解像素和方向对应关系"的能力，地面约束给了算法"锚定脚底真实位置"的能力，人体直立假设给了算法"从脚底推算头顶"的能力。三者缺一不可。
